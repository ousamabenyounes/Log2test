#!/usr/bin/env sh
{% for testId in 1..numberOfTestSuite %}./bin/phpunit  {{ phpunitSuitePath }}/testSuite{{ testId }}{% if  testResultFormat == 'xml' %} --log-junit results{{ testId }}.xml{% elseif testResultFormat == 'json' %} -log-json result{{ testId }}.json {% endif %}
{% endfor %}
