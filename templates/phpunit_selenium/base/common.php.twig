<?php
{{ namespace is defined ? "namespace " ~ namespace ~ ";" : "" }}

require('../BaseTest.php');

/**
 * PhpUnit / Selenium Tests generated from log file -> {{ logFile }}
 * From Line {{ beginLine }} To Line {{ endLine }}
*/

class {{ className }} {{ extends is defined ? "extends " ~ extends : "" }}
{

    public static $browsers = array (
        array (
{% for browser in browsers %}
            'browser' => '{{ browser }}',
{% endfor %}
        ),
    );

    protected function setUp()
    {
        $this->setBrowserUrl('{{ host }}');
        $this->setClassName('{{ className }}');
        $this->createDir('screenshots/{{ className }}');
    }

{% block functions %}
{% endblock %}
}
