{% extends "base/common.php.twig" %}

{% block functions %}
    public function testAllUrls()
    {
{% for path in paths %}
            $this->url('{{ path }}');{% if  pauseBetweenTests > 0 %}

            sleep({{ pauseBetweenTests }});
{% endif %}
{% endfor %}
    }
{% endblock %}
